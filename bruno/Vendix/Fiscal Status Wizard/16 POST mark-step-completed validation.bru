meta {
  name: 16 POST mark-step-completed validation
  type: http
  seq: 16
}

post {
  url: http://{{url}}/store/settings/fiscal-status/invoicing/mark-step-completed
  body: json
  auth: bearer
}

auth:bearer {
  token: {{storeAdminToken}}
}

body:json {
  {
    "step": "validation",
    "ref": {
      "scope": "STORE"
    }
  }
}

tests {
  test("records validation step in completed_steps", function () {
    expect(res.status).to.be.oneOf([200, 201]);
    const body = res.body.data || res.body;
    const completed = body.fiscal_status.invoicing.wizard.completed_steps || [];
    const steps = completed.map(s => s.step || s);
    expect(steps).to.include('validation');
  });
}
