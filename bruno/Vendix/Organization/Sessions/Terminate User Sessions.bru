meta {
  name: Terminate User Sessions
  type: http
  seq: 3
}

delete {
  url: http://{{url}}/organization/sessions/user/1
  body: none
  auth: inherit
}

tests {
  test("Terminate user sessions successful", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('success');
    expect(res.body.success).to.be.true;
  });
}