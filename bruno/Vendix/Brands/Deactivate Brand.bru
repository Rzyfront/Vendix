meta {
  name: Deactivate Brand
  type: http
  seq: 8
}

patch {
  url: http://{{url}}/brands/{{created_brand_id}}/deactivate
  auth: inherit
}

tests {
  test("Deactivate brand successful", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('success', true);
  });
  
  test("Success message is correct", function() {
    expect(res.body.message).to.include('desactivada exitosamente');
  });
}