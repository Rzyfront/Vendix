meta {
  name: 04 GET inventory stock-levels
  type: http
  seq: 4
}

get {
  url: http://{{url}}/organization/inventory/stock-levels
  body: none
  auth: bearer
}

auth:bearer {
  token: {{orgAdminToken}}
}

tests {
  test("200", function () {
    expect(res.status).to.equal(200);
  });
}
