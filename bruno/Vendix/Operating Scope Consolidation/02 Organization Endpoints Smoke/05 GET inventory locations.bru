meta {
  name: 05 GET inventory locations
  type: http
  seq: 5
}

get {
  url: http://{{url}}/organization/inventory/locations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{orgAdminToken}}
}

tests {
  test("200", function () {
    expect(res.status).to.equal(200);
  });
}
