meta {
  name: 13 GET purchase-orders
  type: http
  seq: 13
}

get {
  url: http://{{url}}/organization/purchase-orders
  body: none
  auth: bearer
}

auth:bearer {
  token: {{orgAdminToken}}
}

tests {
  test("200", function () {
    expect(res.status).to.equal(200);
  });
}
