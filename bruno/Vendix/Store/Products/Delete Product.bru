meta {
  name: "Delete Product (Admin Only)"
  type: http
  seq: 12
}

delete {
  url: http://{{url}}/products/{{productId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
