meta {
  name: Verify Domain DNS
  type: http
  seq: 13
}

post {
  url: {{url}}/domains/hostname/techsolutions.myapp.com/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ownerToken}}
}

tests {
  test("Domain DNS verification initiated", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('verification_status');
  });
}