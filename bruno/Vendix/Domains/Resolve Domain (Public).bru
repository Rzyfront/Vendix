meta {
  name: Resolve Domain (Public)
  type: http
  seq: 1
}

get {
  url: http://{{url}}/domains/resolve/:hostname
  body: none
  auth: none
}

params:path {
  hostname: 
}

tests {
  test("Resolve domain successful", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('success', true);
    expect(res.body).to.have.property('data');
  });
}
