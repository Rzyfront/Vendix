meta {
  name: Get Domain by Hostname
  type: http
  seq: 5
}

get {
  url: http://{{url}}/domains/hostname/:hostname
  body: none
  auth: inherit
}

params:path {
  hostname: 
}

tests {
  test("Get domain by hostname successful", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('success', true);
    expect(res.body).to.have.property('data');
  });
}
