# Frontend↔Backend Contract Registry

| Id | Method + route | Request DTO | Response shape | Frontend consumer | Change | Risk | Verification | Status |
|----|----------------|-------------|----------------|-------------------|--------|------|--------------|--------|
| FB-01 | `POST /store/products`, `PUT /store/products/:id` | `Create/UpdateProductDto + tax_inclusive_map?: Record<number, boolean>` | `{ data: product con assignments.is_inclusive }` | `product-create-modal.component.ts:253` | `+ campo mapa; backend lo persiste (hoy lo descarta)` | Mapa ignorado = bug actual; forma inválida | `curl POST con mapa` + `GET` verifica flag guardado | [ ] |
| FB-02 | `GET /store/products/:id` | — | `product_tax_assignments[] + is_inclusive por asignación` | `product-create-modal.component.ts:321` (hidrata chips) | `+ flag por asignación; chips leen asignación, no catálogo` | Hidratar de catálogo reintroduce el bug | Abrir detalle tras guardar: chip conserva valor | [ ] |
| FB-03 | `POST /store/products/bulk-edit` | `changes.tax_category_action + inclusive?: Record<number, boolean>` | `{ data: BulkEditResult }` | `bulk-edit-field-control + page` (emite mapa) | `+ inclusivo opcional por catId en la acción` | `ids` sin entrada en mapa = default catálogo | `curl bulk replace con mapa` + `GET` productos | [ ] |
| FB-04 | `POST /store/products/bulk-edit/preview` | mismo `changes` | preview con impuestos por producto | `products-bulk-edit.service.ts:previewInBatches` | preview refleja inclusivos (no suma encima) | Preview diverge del apply | preview vs apply en mismo lote, diff 0 | [ ] |
| FB-05 | `GET` storefront producto/precios | — | precio + desglose con flag por línea | `storefront-price.service.ts` + tienda pública | vitrina no suma inclusivo; muestra "IVA incluido" | Doble cómputo con checkout | precio vitrina == total checkout misma línea | [ ] |
| FB-06 | checkout web / WhatsApp / cobro POS | carrito + líneas | totales + `taxes[]` con base e impuesto | checkout, order-flow, POS | inclusivo: total == precio publicado; impuesto despejado | Crecer total = regresión del bug | spec por canal: total == precio con inclusivo 19% | [ ] |
| FB-07 | emisión factura desde venta | snapshot de línea | `invoice_items/invoice_taxes.is_inclusive` + base despejada | invoice-flow / invoicing | heredar flag de la asignación al emitir | Factura suma encima (defecto CP-facturacion) | factura de venta inclusiva: total == precio | [ ] |
