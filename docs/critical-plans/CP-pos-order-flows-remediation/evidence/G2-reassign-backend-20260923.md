# G.2 — endpoint backend de reasignación (2026-09-23)

ADR-07/04 aceptados. `POST /store/table-sessions/reassign` recibe solo `{order_id,target_table_id}` con DTO validado y permiso `store:table_sessions:update`. `reassignSessionToTable` resuelve la orden bajo scope, toma `lockOrderLifecycle` y vuelve a leer sesiones, pagos, facturas y mesa en la misma transacción. Exige la política G.1. La mesa debe estar `available`; la reclama con `updateMany status=available` antes de crear la sesión. Crea una nueva `table_sessions` para **la misma orden**, conserva la antigua cerrada, estampa los tickets KDS al destino, deja la mesa ocupada y emite SSE solo después del commit. No re-dispara cocina, no toca inventario ni estado de orden.

`occupied` devuelve `TABLE_SESSION_ALREADY_OPEN`; `reserved`/`cleaning` devuelven `TABLE_INVALID_STATUS`; carrera de estado y P2034 devuelven `SYS_CONFLICT_001`; P2002 se traduce a `TABLE_SESSION_ALREADY_OPEN`. Pruebas red→green, incluida carrera de mesa y estados reservada/limpieza. Jest exacto `table-sessions.service.spec.ts` + `table-sessions.controller.spec.ts`: **80/80** green, `git diff --check` limpio. Runtime curl/SQL y UI del modal pendientes.
