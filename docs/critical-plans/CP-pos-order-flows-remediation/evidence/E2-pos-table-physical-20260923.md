# E.2 — cobro POS de mesa con producto físico (QA local)

Tienda #10, mesa #26 en `cleaning`, producto físico #421 x1, efectivo #5, nombre de referencia QA. `POST /store/payments/pos` con `table_id:26` devolvió **201** y creó la orden **#1177** / sesión **#120**. Orden `finished`, total/pagado $5.000; una fila de pago `succeeded`, sesión con `paid_at` y sin `closed_at`, mesa `occupied`. Inventario: ítem **#1900** `inventory_committed=true`, reserva de orden **#510** `consumed` por una unidad y una transacción **#2013** `quantity_change=-1`; stock físico de producto #421/ubicación #50 bajó 3992→3991, reservado se mantuvo en 2 por otras órdenes QA. No hubo segundo descuento.

Repetir el mismo payload POS respondió **409 `POS_TABLE_SESSION_ALREADY_CHARGED`**; siguieron un pago y una transacción negativa. `POST /store/table-sessions/120/close` respondió **201** y movió la mesa a `cleaning` con `closed_at` poblado. Los cuerpos locales están en `/tmp/e2-pos-table-physical.*`. Es prueba física del carril **mesa**; el carril **split** está en `E2-split-draft-20260923.md`. E.2 conserva abierto el barrido global DB-12/DB-27; no se infiere que un solo ítem pruebe todas las órdenes históricas.
