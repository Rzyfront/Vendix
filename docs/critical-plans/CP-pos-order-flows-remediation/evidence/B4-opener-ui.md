# B.4 Mesero = quien abrió la sesión

Tienda QA local #10, mesa #24/sesión #116: SQL `table_sessions.opened_by=162` → usuario **Andrés Roku Owner**. `GET /store/table-sessions/116` 200 proyecta `table.waiter={id:162,first_name:'Andrés',last_name:'Roku Owner'}`; `GET /store/tables/floor-map` 200 proyecta el mismo `active_session.waiter` y `opened_by=162`. `GET /store/orders/1134` conserva el precedente `table_sessions[0].opener` con el mismo nombre. En la UI real de `https://vendix.com/admin/restaurant-ops/tables`, el tile muestra «Andrés Roku Owner» y `aria-label` incluye «Mesero: Andrés Roku Owner» (`B4-opener-floor.png`); clic navega a `/admin/restaurant-ops/tables/session/116` y el encabezado dice «Sesión #116 — Cuenta abierta — Mesero: Andrés Roku Owner» (`B4-opener-session.png`). Sin escritura de QA para este recorrido.

El pivote `table_waiters` permanece para **asignaciones estáticas**, no para esta proyección (`B4-pivot-consumers.txt`). La prueba backend `findOne proyecta table.waiter desde opener, no desde table_waiters` usa opener y asignado distintos; otras pruebas de impresión POS/KDS cubren reimpresión y opener. El agente paralelo agregó 4 tests de página/tile (`67e8d462b`) para nombre y QR anónimo sin opener. Verificación: frontend **11/11**; backend print **39/39**, tables **5/5**, table sessions **57/57**. API sin auth 401 y sesión desconocida 404. El watcher Angular y health backend estaban OK.

`opened_by=NULL` puede ser legítimo para QR anónimo; no se inventa un mesero ni se hace backfill. La consulta local de tienda #10 halló 0 sesiones abiertas con orden cancelada y 0 openers nulos, pero esto no se extrapola a otras tiendas. El `include` de opener está acotado y no se consulta una vez por tile.
