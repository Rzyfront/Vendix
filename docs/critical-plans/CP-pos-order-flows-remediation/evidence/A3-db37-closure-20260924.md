# A.3 DB-37 cierre + higiene de símbolos — 2026-09-24

Step: A.3 (CP-pos-order-flows-remediation). Rama: `develop`. Checkout compartido, solo lectura en código.
Evidencia: `A3-db37-closure.sql`, `A3-db37-closure.txt` (mismo directorio).

## 1. DB-37 — invoices aceptadas vs líneas canceladas después: 0 filas (CIERRA)

Comando (contrato del step):

```bash
docker exec vendix_postgres psql -U username -d vendix_db -c "SELECT i.id FROM invoices i JOIN order_items x ON x.order_id=i.order_id WHERE i.status='accepted' AND x.cancelled_at>i.updated_at;"
```

Salida:

```text
 id
----
(0 rows)
```

Ampliado solo-lectura (id+timestamps, trazabilidad; también 0 filas):

```bash
docker exec vendix_postgres psql -U username -d vendix_db -c "SELECT i.id AS invoice_id, i.status, i.updated_at AS invoice_updated_at, x.id AS order_item_id, x.order_id, x.cancelled_at FROM invoices i JOIN order_items x ON x.order_id=i.order_id WHERE i.status='accepted' AND x.cancelled_at>i.updated_at ORDER BY i.id, x.id;"
```

```text
 invoice_id | status | invoice_updated_at | order_item_id | order_id | cancelled_at
------------+--------+--------------------+---------------+----------+--------------
(0 rows)
```

**Veredicto: DB-37 da 0 → fila lista para [x].** Sin hallazgo, sin tocar datos.

## 2. Higiene — `cancelDeliveredOrderItem` sin `payment_status` / `'completed'` literales

Método: `apps/backend/src/domains/store/orders/order-flow/order-flow.service.ts:2923-3182`
(siguiente método `revertKitchenOrderDelivery` en L3184).

Comando:

```bash
sed -n '2923,3180p' apps/backend/src/domains/store/orders/order-flow/order-flow.service.ts | grep -n "payment_status\|'completed'"
# → NO_MATCH_IN_RANGE (0 apariciones, ni siquiera en comentarios dentro del rango)
sed -n '2923,3182p' apps/backend/src/domains/store/orders/order-flow/order-flow.service.ts | grep -n "payment_status\|'completed'\|new Set\|succeeded\|captured"
# → sin matches (0 duplicados del set literal)
```

`payment_status` solo aparece fuera del método, en comentarios/contrato de hermanos
(L1896, L2577-2578 explicando que la columna no existe y que la evidencia son las filas
de `payments`, igual que en `cancelDeliveredOrderItem` ADR-02). Dentro del método: 0.

Consumo centralizado (no duplicado):

- Export: `apps/backend/src/domains/store/orders/order-flow/order-cancellation-policy.util.ts:40-42`

```ts
export const SETTLED_PAYMENT_STATES: ReadonlySet<string> = new Set([
  'succeeded', 'captured', 'partially_refunded', 'refunded',
]);
```

- Import: `order-flow.service.ts:6` (`SETTLED_PAYMENT_STATES` desde `./order-cancellation-policy.util`).
- Uso pre-tx: `order-flow.service.ts:2943-2945` (`order.payments?.some(... SETTLED_PAYMENT_STATES.has(payment.state))` → lanza `ORD_ITEM_CANCEL_PAID_001`).
- Uso en-tx bajo lock: `order-flow.service.ts:3034-3037` (`tx.payments.findFirst({ where: { order_id, state: { in: [...SETTLED_PAYMENT_STATES] } } })` → lanza `ORD_ITEM_CANCEL_PAID_001`).
- Hermanos que reutilizan el mismo símbolo (no duplican): L2580, L2705, L3818.

**Veredicto higiene: OK — 0 literales funcionales, set único exportado y consumido.**

## 3. Mapeo ES con CTA reembolso en `error-messages.ts`

Archivo: `apps/frontend/src/app/core/utils/error-messages.ts:275-278`

```ts
ORD_ITEM_CANCEL_PAID_001:
  'Esta orden ya fue cobrada. Usa Reembolso para devolver un plato.',
ORD_ITEM_CANCEL_STATE_001:
  'Esta orden está cerrada y no admite cancelar platos. Revisa su estado antes de continuar.',
```

- `ORD_ITEM_CANCEL_PAID_001` (L275-276): ES + CTA explícita a Reembolso. Idéntico al `devMessage` backend y a los throws del método (L2948, L3044).
- `ORD_ITEM_CANCEL_STATE_001` (L277-278): ES, copy de estado cerrado sin CTA de reembolso (correcto: el rechazo es por estado, no por cobro).

Respaldo backend: `apps/backend/src/common/errors/error-codes.ts:5702-5711`
(`ORD_ITEM_CANCEL_PAID_001` 409 + `ORD_ITEM_CANCEL_STATE_001` 409, ambos registrados).

**Veredicto mapeo: OK.**
