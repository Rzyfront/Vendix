# E.2 Reserva del borrador POS bajo claim, sin doble cobro

QA local tienda #10/producto físico #421 (track_inventory=true, precio $5.000, stock disponible 3995). **El carril relevante es `POST /store/payments/pos` con `is_draft=true`**: orden #1140 nació `draft` sin `stock_reservations`. El POST `flow/pay` efectivo devolvió **200**; quedó exactamente una reserva activa #495 por una unidad, un pago succeeded #830 por $5.000, orden `processing` con `total_paid=grand_total=5000`. `stock_levels` del lugar principal cambió `quantity_reserved:0→1`, `quantity_available:3995→3994`, mientras `quantity_on_hand` siguió 3995. Auditoría `order.promoted_to_created` registró `reservation_count=1`. El consumo de inventario aún no ocurre porque la orden sigue en processing.

Segunda orden POS draft #1142: cero reservas antes del cobro. Dos POST `flow/pay` lanzados en paralelo devolvieron **409** `ORD_FLOW_PAYMENT_FAILED_001` (`reason=lost_pay_order_claim_race`) y **200**, respectivamente. SQL posterior: **un** pago succeeded, **una** reserva activa y auditoría de promoción con `reservation_count=1`. No hay doble consumo ni transacción negativa para sus ítems todavía (`E2-flowpay-reservation.sql/txt`). Nota de scope: estos dos audit_logs tienen `store_id` de columna nulo, pero `metadata.store_id=10`; el SQL filtra por metadata y orden.

Contraejemplo de fixture: `POST /store/orders` draft #1138 ya había creado reserva al guardar; se canceló auditadamente, con reserva cancelada. Usarlo habría dado un falso verde para E.2. Los dos borradores POS cobrados #1140/#1142 permanecen como órdenes QA locales en processing, sin tocar producción. Aún falta demostrar el salto a remisión y el commit de inventario sin doble descuento tras fulfillment; el paso sigue `in-progress`.
