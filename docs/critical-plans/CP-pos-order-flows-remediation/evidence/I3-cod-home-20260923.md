# I.3 — contra-entrega a domicilio y remisión (QA local)

Tienda #10, POS, método de pago #25 con `processing_mode=ON_DELIVERY`, método de envío #9 (`own_fleet`), servicio #425, alias QA y snapshot de dirección. `POST /store/payments/pos` respondió **201** y creó orden **#1167** / pago **#843**. SQL posterior: `orders.state=pending_payment`, `delivery_type=home_delivery`, `grand_total=10000`, `total_paid=0`, `remaining_balance=10000`, `payments.state=pending`, `amount=10000`, `shipping_method_id=9`. El efectivo no se reconoció en caja al crearla.

`POST /store/dispatch-notes/from-order/1167` exige `items[]` en el DTO: el intento con cuerpo vacío devolvió **400 `SYS_VALIDATION_001`**, `items must be an array` y no creó remisión. Reintento con `{"items":[{"order_item_id":1890,"dispatched_quantity":1}],"target_status":"draft"}` devolvió **201** y creó remisión **#227** en estado `draft`; orden y pago permanecieron `pending_payment` / `pending` con saldo vivo. El cobro de mostrador #1115 ya está en `evidence/I3-cod-counter.txt`. SQL de solo lectura sobre órdenes de tienda #10 creadas desde el 23-09-2026: **0** `orders.finished` con `payments.pending`.

PaymentsService spec **108/108** (incluye `ON_DELIVERY` para `home_delivery` y `direct_delivery`, efectivo/tarjeta directas, método legado sin `processing_mode` y dos negativos de método). Backend health 200 y frontend watcher OK. Sin token: POST POS 401. El hallazgo de método inexistente #99999999 que antes daba 500 `SYS_INTERNAL_001` se corrigió en `4ef91b0e7`: ahora responde **400 `PAY_METHOD_DISABLED_001`** con `details.reason=payment_method_not_found`; método omitido responde 400 `payment_method_required`, y método #14 de tienda #3 usado con token de tienda #10 responde 400 `payment_method_not_found`. SQL: cero órdenes con los alias QA de las tres sondas, orden COD #1167 conserva `pending_payment` / pago `pending` y saldo $10.000. La sonda inversa con token de tienda #3 fue detenida antes por gate de suscripción 403 `SUBSCRIPTION_003`; no se usa para afirmar aislamiento. Todos los datos son locales, no producción.
