# H.3 Mesas UI vs POS UI, mismo producto sin asignación

`https://vendix.com/admin/restaurant-ops/tables`: mesa QA #15 abierta como sesión #108/orden draft #1125. En la página de mesa se eligió **Agregar items → Coca-Cola 400ml (producto 302) → Agregar**; Network `POST /store/table-sessions/108/add-items` → 201 y pantalla mostró total $38.000. Para hacer visible Cobrar en esta seed local se cambió temporalmente `restaurant.enable_table_checkout=false→true`, se inició sesión nueva en la UI, y se restauró a `false` al acabar. **Opciones → Cobrar → Efectivo → Cobrar $38.000** envió `POST /api/store/payments/pos` con `table_session_id:108`, subtotal/total 38000, método 5, monto recibido 38000, **sin propiedad `items`**. La primera solicitud coincidió con reinicio watch del backend y no escribió pago; la segunda respondió HTTP **201**, orden **1125**, pago **821**. El plano volvió a mostrar la mesa `OCUPADA · Pagada` (`H3-ui-mesas-taxless.png`).

`H3-ui-mesas-taxless.sql/txt` compara con el POS «Consumo en mesa» previo, orden #1124: ambas órdenes/mesas tienen **una línea producto 302**, subtotal/total 38000, impuesto 0, cero `order_item_taxes`, un pago succeeded y `paid_at` presente con sesión abierta. La configuración temporal quedó restaurada (`enable_table_checkout=false`). No hubo 422 fiscal en ninguno de los dos recorridos visuales.
