# E.4 — cobro/cancelación/finalización desde detalle (QA local, 2026-09-23)

No se empuja ni se valida producción en este lote. Commits de código: `93cb5da23` (toast de platos frescos), `19848ae4d` + `df42c55e2` (restauración del estado previo si `flow/pay` falla antes de persistir pago); `7687a6a5d` corrige el 200-sobre-404 detectado en la sonda cruzada.

| Esquema | Sonda | Resultado real |
| --- | --- | --- |
| Happy | Detalle de borrador #1163, acción Cancelar | UI llama `flow/cancel` 200 y muestra CANCELADA; captura `E4-ui-cancel-done.png`. |
| Happy | `dine_in` #1166 sin método: intento con método inválido → reintento válido #5 → finalizar | Primero 409 `ORD_FLOW_PAYMENT_FAILED_001` / `payment_method_not_found`, orden vuelve a `created` y 0 pagos; después `flow/pay` 200, estado `processing` y un pago; `confirm-delivery` 200, estado `finished` y un pago. |
| Happy | Fiado de mesa y cuatro carriles de cobro | Matriz previa `A2-four-lane-matrix.md`: fiado #1146 200, POS directo / borrador reabierto / detalle / orden adoptada. **No** se repitieron los cuatro carriles de UI en esta sesión. |
| Sad | `home_delivery` físico #1162 sin método | `flow/pay` 422 `ORD_SHIP_CHARGE_001`, orden queda `created`, 0 pagos. Fixture se canceló. |
| Sad | Cancelar orden terminada #1157 | `flow/cancel` 400 `ORD_STATUS_001`, `details.state=finished`. Falta prueba específica de `refunded`. |
| Sad | Finalizar con plato preparado pendiente #1056 | `confirm-delivery` 422 `ORDER_HAS_PENDING_KITCHEN_ITEMS`; `details.pending_items` trae Pollo Árabe E2E, variante Picante, cantidad 1, estado ready. La UI muestra el nombre/variante real sin toast de éxito; captura `E4-ui-pending-finish.png`. |
| Brute | Cancelaciones simultáneas de borrador #1164 | HTTP 200 + 400 `ORD_STATUS_001`; estado final `cancelled`, sin segunda transición. |
| Brute | DELETE de orden pagada #1141 | HTTP 400 `ORD_VALIDATE_001`, `details.reason=financial_evidence`; no se agregó llamador DELETE al frontend. |
| Brute | POST cancel sin autenticación | HTTP 401; la orden pagada quedó intacta. |
| Brute | GET detalle #1166 desde tienda ajena | Antes del fix: HTTP 200 con cuerpo 404 `ORD_FIND_001` sin fuga de datos. Tras `7687a6a5d`: HTTP 404 real `ORD_FIND_001`; dueño HTTP 200, sin sesión HTTP 401. `orders.controller.spec.ts` 12/12. |

Verificación enfocada: `order-flow.service.spec.ts` 121/121 (agente E.4), `order-details-page.component.spec.ts` 3/3 (orquestador), frontend watcher último ciclo OK y backend `API_READY`/health OK. Los archivos crudos locales están en `/tmp/e4-*` mientras dure esta máquina. No se corrigió por SQL la orden QA pre-fix #1165 (`finished`, cero pagos); no es un caso nuevo tras el fix. **Pendiente para cerrar E.4**: recorrido fresco mesa→pedido→detalle→cobro y prueba de cancelación `refunded`; el resto del bundle aún no está listo para desplegar.
