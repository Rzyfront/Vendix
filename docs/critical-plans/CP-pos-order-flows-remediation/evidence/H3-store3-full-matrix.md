# H.3 Matriz sin asignación en tienda fiscal LOCKED

Tienda seed 3/producto servicio 426 con cero `product_tax_assignments`. Cuatro carriles: POS sin mesa orden #1110, POS `table_id` orden #1111, Mesas `add-items` + pago sin `items` orden #1127/sesión #110, POS con sesión preexistente + `items` orden #1128/sesión #111. Todos HTTP 201, total 50000/IVA 0, exactamente un pago succeeded y cero snapshots fiscales por orden (`H3-store3-full-matrix.sql/txt`). Para las escrituras QA locales, la suscripción seed #4 pasó temporalmente `expired→active`; cada bloque tenía `trap EXIT` que restauró `expired`, y `sub:features:3` quedó ausente en Redis. Un intento intermedio devolvió 502 por reinicio watch, sin escritura; fue repetido con health200. No se cambió el estado fiscal LOCKED. La tienda 10 con área fiscal INACTIVE tiene los mismos cuatro carriles en `H3-*` anteriores.
