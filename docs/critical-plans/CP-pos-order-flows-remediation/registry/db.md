# Database Contract Registry

| Id | Model / table | Columns | R/W | Tenant scoping | Migration | Consumers | Invariant | Verification | Status |
|----|---------------|---------|-----|----------------|-----------|-----------|-----------|--------------|--------|
| DB-01 | `orders` | `state`, `internal_notes._flow_metadata` | RW | `store_id` directo | ninguna | `order-flow.service.ts:4170` | `delivered→processing` solo nace del puente KDS | `SELECT count(*) FROM orders WHERE internal_notes LIKE '%"from":"delivered","to":"processing"%'` estable | [ ] |
| DB-02 | `orders` | `subtotal_amount`, `tax_amount`, `grand_total` | W | `store_id` directo | ninguna | `order-flow.service.ts:2465`, `:2199` | `grand_total ≥ Σ payments succeeded\|captured` | `SELECT p.order_id FROM payments p JOIN orders o ON o.id=p.order_id WHERE p.state IN ('succeeded','captured') GROUP BY p.order_id,o.grand_total HAVING SUM(p.amount)>o.grand_total+.01` = 0 | [ ] |
| DB-03 | `orders` | `total_paid`, `remaining_balance` | RW | `store_id` directo | ninguna | `payments.service.ts:4591`, `refund-calculation:107` | `total_paid = Σ payments succeeded\|captured` | `SELECT p.order_id FROM payments p JOIN orders o ON o.id=p.order_id WHERE p.state IN ('succeeded','captured') GROUP BY p.order_id,o.total_paid HAVING abs(SUM(p.amount)-o.total_paid)>.01` = 0 | [ ] |
| DB-04 | `orders` | `delivery_type`, `channel` | W | `store_id` directo | ninguna | `orders.service.ts:416-448` (hoy NO los escribe — F-013) | toda orden persiste el `delivery_type` del DTO, no el default del schema | `SELECT delivery_type,channel,count(*) FROM orders WHERE created_at>:deploy GROUP BY 1,2` incluye `dine_in` y `home_delivery` | [ ] |
| DB-05 | `orders` | `delivery_type='pickup'` | W | `store_id` directo | ninguna | `pos-checkout-shell.component.ts:1286` (ADR-01) | ninguna orden POS nueva nace `pickup` | `SELECT count(*) FROM orders WHERE channel='pos' AND delivery_type='pickup' AND created_at>:deploy` = 0 | [ ] |
| DB-06 | `orders` | `customer_id`, `customer_alias` | W | `store_id` directo; CHECK `orders_customer_xor_alias` | ninguna | `orders.service.ts:419`, `payments.service.ts:855` | nunca ambos poblados a la vez | `SELECT count(*) FROM orders WHERE customer_id IS NOT NULL AND customer_alias IS NOT NULL` = 0 | [ ] |
| DB-07 | `orders` | `shipping_address_id`, `shipping_address_snapshot` | W | `store_id` directo | ninguna | `dispatch-notes.service.ts:2026` (FK) + detalle (snapshot) | toda venta con envío tiene FK **y** snapshot (ADR-05) | `SELECT count(*) FROM orders WHERE delivery_type='home_delivery' AND (shipping_address_id IS NULL OR shipping_address_snapshot IS NULL) AND created_at>:deploy` = 0 | [ ] |
| DB-08 | `order_items` | `delivered_at`, `delivered_by_user_id` | W | rel. `orders.store_id` | ninguna | `order-flow.service.ts:1858`, `kitchen-fire.service.ts:2708`, `dispatch-note-events.listener.ts:499` | `delivered_at` monótono: solo se estampa donde es NULL | `SELECT count(*) FROM order_items WHERE delivered_at>updated_at` = 0 | [ ] |
| DB-09 | `order_items` | `cancelled_at`, `cancellation_reason`, `cancellation_type` | W | rel. `orders.store_id` | ninguna | `order-flow.service.ts:2427`, `:3250` | `cancellation_type ∈ {before_fire, after_fire_reused, after_fire_waste}`, el vocabulario de `POST_CANCEL_REMAKE_TYPES:1259` | `SELECT DISTINCT cancellation_type FROM order_items WHERE cancelled_at IS NOT NULL` | [ ] |
| DB-10 | `order_items` | `is_takeaway` | R | rel. `orders.store_id` | ninguna | `kitchen-fire.service.ts:2656`, `pos-cart.service.ts:2464` | el badge KDS pasa a `orders.delivery_type`; el booleano no cambia de valor ni de reglas (F-030) | `SELECT is_takeaway,count(*) FROM order_items GROUP BY 1` idéntico antes/después | [ ] |
| DB-11 | `order_items` | `inventory_consumed_at_fire` | RW | rel. `orders.store_id` | ninguna | `kitchen-fire.service.ts:876`, `payments.service.ts` updateInventoryFromOrder | nunca pasa de `true` a `false` ni se re-dispara (ADR-07) | `SELECT count(*) FROM order_items WHERE inventory_consumed_at_fire` no decrece entre snapshots | [ ] |
| DB-12 | `order_items` | `inventory_committed`, `inventory_committed_at` | W | rel. `orders.store_id` | ninguna | `order-stock-commit.service.ts:434` (claim atómico) | un ítem descuenta stock una sola vez | `SELECT it.order_item_id FROM inventory_transactions it WHERE it.quantity_change<0 GROUP BY 1 HAVING count(*)>1` = 0 | [ ] |
| DB-13 | `order_item_taxes` | `tax_amount`, `tax_rate_id`, `tax_type` | R/W | anidado en `order_items` de la tienda | ninguna | `table-sessions.addItems` y `payments.buildPosOrderItem` | una línea vieja conserva sus filas; nueva taxless no necesita fila; `orders.tax_amount` = suma activa | SQL por `order_id` antes/después del cobro, comparando IDs/tipos/montos | [ ] |
| DB-14 | `payments` | `state`, `amount`, `paid_at` | W | rel. `orders.store_id` | ninguna | `order-flow.service.ts:973-1006`, `payments.service.ts:4562` | `flow/pay` rechaza el segundo cobro íntegro: `Σ succeeded\|captured ≤ grand_total` (F-003) | `SELECT order_id FROM payments WHERE state IN ('succeeded','captured') GROUP BY 1 HAVING count(*)>1` cruzado con `grand_total` | [ ] |
| DB-15 | `payments` | `financial_account_id`, `financial_idempotency_key` | W | rel. `orders.store_id` | ninguna | `split-account-payment.service.ts:442` | la llave de idempotencia es única: un reintento no duplica el abono | `SELECT financial_idempotency_key FROM payments WHERE financial_idempotency_key IS NOT NULL GROUP BY 1 HAVING count(*)>1` = 0 | [ ] |
| DB-16 | `table_sessions` | `table_id`, `order_id`, `opened_by`, `opened_at`, `closed_at` | R/W | `store_id` directo | ninguna | `table-sessions.service.ts`, A.4 `cancelOrder` | sesión abierta nunca apunta a orden recién cancelada; nueva sesión lleva `opened_by` | SQL sesión abierta + `orders.state='cancelled'` = 0 | [ ] |
| DB-17 | `table_sessions` | `paid_at` | W | `store_id` | ninguna | ADR-03: `payments.service.ts:3901`, `split-account-payment:547`, `webhook-handler:629`, `order-flow:646` (no escribe; sí tras B.2) | `paid_at NOT NULL ⟺ Σ pagos ≥ total` | `SELECT ts.id FROM table_sessions ts JOIN orders o ON o.id=ts.order_id WHERE ts.paid_at IS NULL AND o.total_paid>=o.grand_total AND o.grand_total>0`=0 | [ ] |
| DB-18 | `table_sessions` | `closed_at` | W | `store_id` directo | ninguna | `table-sessions.service.ts:1140`; `webhook-handler.service.ts:634` deja de cerrar | ningún cobro cierra la sesión: cerrar es acto del mesero (ADR-03) | `SELECT count(*) FROM table_sessions WHERE closed_at-paid_at<interval '2 s' AND closed_at>:deploy` = 0 | [ ] |
| DB-19 | `table_sessions` | índice único parcial `table_sessions_one_open_per_table` (`table_id` WHERE `closed_at IS NULL`) | R | invariante de BD, no de aplicación | ninguna (`20260829094000`) | `table-sessions.service.ts:417`; ADR-07 | como máximo una sesión abierta por mesa | `SELECT table_id FROM table_sessions WHERE closed_at IS NULL GROUP BY 1 HAVING count(*)>1` = 0 | [ ] |
| DB-20 | `table_sessions` | `order_id` (NO único: varias sesiones por orden) | W | `store_id` directo | ninguna | ADR-07; guard `orders.service.ts:1826` pasa a «sin sesión abierta» | una orden reasignada conserva la cerrada y sigue editable | `SELECT order_id FROM table_sessions GROUP BY 1 HAVING count(*)>1` + `PATCH /store/orders/:id` = 200 | [ ] |
| DB-21 | `tables` | `status` | W | `store_id` directo | ninguna | `table-sessions.service.ts:432` (setter, no transición), `:1144` | el cobro nunca mueve `status`; `cleaning→occupied` avisa, no bloquea (F-020) | `SELECT t.id FROM tables t JOIN table_sessions s ON s.table_id=t.id AND s.closed_at IS NULL WHERE t.status<>'occupied'` = 0 | [ ] |
| DB-22 | `kitchen_tickets` | `status`, `table_id`, `ready_at` | W | `store_id` directo | ninguna | `kitchen-fire.service.ts:2695`; `table-sessions:1317` | tras reasignar mesa, todo ticket apunta a la mesa vigente | `SELECT k.id FROM kitchen_tickets k JOIN table_sessions s ON s.order_id=k.order_id AND s.closed_at IS NULL WHERE k.table_id IS DISTINCT FROM s.table_id` = 0 | [ ] |
| DB-23 | `kitchen_ticket_items` | `status` (sin `delivered_at` propio) | W | rel. `kitchen_ticket.store_id` | ninguna | `kitchen-fire:2708`, `order-flow.service.ts:1904` | `delivered_at NOT NULL` ⇒ ticket item en `delivered` | `SELECT k.id FROM kitchen_ticket_items k JOIN order_items i ON i.id=k.order_item_id WHERE i.delivered_at IS NOT NULL AND k.status<>'delivered'` = 0 | [ ] |
| DB-24 | `kds_sessions` | `status`, `last_seen_at` | R | `store_id` directo | ninguna | `kitchen-fire.service.ts:2611` (`assertCanMutateStationTicket`); el seam de orden NO la lee (ADR-06) | la entrega del mesero no depende del turno de estación | `grep -c kdsSessionsService order-flow.service.ts` = 0 + entrega OK con turno ajeno `open` | [ ] |
| DB-25 | `addresses` | create con `user_id = NULL`, `store_id` NOT NULL | W | `store_id` directo | ninguna | `addresses.service.ts:96`; remisión y mapa resuelven por FK (ADR-05) | toda dirección huérfana la referencia una orden con alias | `SELECT a.id FROM addresses a WHERE a.user_id IS NULL AND NOT EXISTS (SELECT 1 FROM orders o WHERE o.shipping_address_id=a.id)` = 0 | [ ] |
| DB-26 | `addresses` | `is_primary` (`updateMany` de `unsetOtherDefaults`) | W | `store_id` directo; hoy el `updateMany` no filtra `user_id` nulo | ninguna | `addresses.service.ts:96-102` y `:288-305` (mina F-024) | una dirección sin cliente nunca es `is_primary` | `SELECT count(*) FROM addresses WHERE is_primary AND user_id IS NULL` = 0 y `count(*) WHERE is_primary` estable tras el POST | [ ] |
| DB-27 | `stock_reservations` | `status`, `quantity` | W | rel. `inventory_locations` | ninguna | `order-flow.service.ts:487`, `order-stock-commit:453` | orden promovida de `draft` con líneas físicas deja ≥1 reserva activa | `SELECT o.id FROM orders o WHERE o.state<>'draft' AND NOT EXISTS (SELECT 1 FROM stock_reservations r WHERE r.reserved_for_type='order' AND r.reserved_for_id=o.id)` | [ ] |
| DB-28 | `stock_levels` | `quantity_on_hand`, `quantity_reserved`, `quantity_available` | W | rel. `inventory_locations.store_id` | ninguna | `StockLevelManager.updateStock` desde `order-flow.service.ts:2409` | cancelar un plato preparado NO devuelve el producto vendido (ADR-08) | `SELECT quantity_on_hand FROM stock_levels WHERE product_id=:platoPreparado` idéntico antes/después | [ ] |
| DB-29 | `inventory_movements` | `movement_type`, `source_module` | W | rel. OR `products`\|locations | ninguna | `order-flow.service.ts:2414`, `:3230` | el restock no toca productos `prepared` | `SELECT m.id FROM inventory_movements m JOIN products p ON p.id=m.product_id WHERE m.source_module='order_item_cancel_delivered' AND p.product_type='prepared'` = 0 | [ ] |
| DB-30 | `inventory_transactions` | `quantity_change`, `order_item_id`, `kds_session_id` | RW | rel. `products.store_id` | ninguna | fire (consumo BOM) y `order-flow.service.ts:3213` (reversa `reuse`) | reversa `reuse`: `Σ quantity_change` por hoja del BOM vuelve a 0 | `SELECT product_id FROM inventory_transactions WHERE order_item_id=:id GROUP BY 1 HAVING SUM(quantity_change)<>0` = 0 | [ ] |
| DB-31 | `accounting_entries` | `source_type`, `source_id`, `total_debit` / `total_credit` | W | entidad fiscal + `store_id` | ninguna | `auto-entry.service.ts:4584` escribe `source_type='inventory.adjusted'` | asiento de merma cuadrado | `SELECT id FROM accounting_entries WHERE source_type='inventory.adjusted' AND total_debit<>total_credit` = 0 | [ ] |
| DB-32 | `accounting_entry_lines` | `account_id` → PUC `5295` / `1435` | W | rel. `entry.organization_id` | ninguna | `account-mapping.service.ts:279` (`inventory.adjusted.shrinkage`) | merma: DR 5295 / CR 1435, fuera del COGS de la venta | `SELECT c.code,l.debit_amount,l.credit_amount FROM accounting_entry_lines l JOIN chart_of_accounts c ON c.id=l.account_id WHERE l.entry_id=:entryId` | [ ] |
| DB-33 | `cash_register_movements` | `type`, `amount`, `order_id`, `payment_id` | W | `store_id` directo | ninguna | `order-flow.service.ts:3407` | cancelar venta cobrada en efectivo deja egreso = Σ pagos en efectivo | `SELECT o.id FROM orders o WHERE o.state='cancelled' AND NOT EXISTS (SELECT 1 FROM cash_register_movements m WHERE m.order_id=o.id AND m.type='cash_out')` = 0 | [ ] |
| DB-34 | `refunds` | `state`, `amount`, `refund_method` | W | rel. `orders.store_id` | ninguna | `order-flow.service.ts` (hoy **cero** `refunds.create`) | cancelar orden con pago `succeeded` crea ≥1 `refunds` | `SELECT o.id FROM orders o JOIN payments p ON p.order_id=o.id AND p.state='succeeded' WHERE o.state='cancelled' AND NOT EXISTS (SELECT 1 FROM refunds r WHERE r.order_id=o.id)` = 0 | [ ] |
| DB-35 | `dispatch_notes` | `order_id`, `customer_id`, `customer_name`, `customer_address` | W | `store_id` directo | ninguna | `dispatch-notes.service.ts:2026-2032` | una orden con alias genera remisión: el snapshot suple al cliente ausente | `SELECT count(*) FROM dispatch_notes d JOIN orders o ON o.id=d.order_id WHERE o.customer_alias IS NOT NULL AND d.customer_address IS NULL` = 0 | [ ] |
| DB-36 | `dispatch_route_stops` | `dispatch_note_id`, `status`, `collected_amount` | R | **sin scope auto** (getter sin registro) → filtro manual por `route.store_id` | ninguna | `route-flow.service.ts:351` | toda parada resuelve coordenadas por FK o snapshot | `SELECT s.id FROM dispatch_route_stops s JOIN dispatch_notes d ON d.id=s.dispatch_note_id WHERE d.customer_address IS NULL` = 0 | [ ] |
| DB-37 | `invoices` | `status`, `invoice_number`, `financial_account_id` | R | `store_id` directo | ninguna | `invoicing.service.ts:2375`; guard ADR-02 antes de recalcular | ninguna línea desaparece de una orden ya facturada | `SELECT i.id FROM invoices i JOIN order_items x ON x.order_id=i.order_id WHERE i.status='accepted' AND x.cancelled_at>i.updated_at` = 0 | [ ] |
| DB-38 | `order_financial_accounts` | `grand_total`, `paid_snapshot` | RW | `store_id` directo | ninguna | `split-order.service.ts:192`, `split-account-payment.service.ts:442` | `Σ accounts.grand_total = orders.grand_total` del split activo | `SELECT SUM(grand_total) FROM order_financial_accounts WHERE split_id=:s` = `orders.grand_total` | [ ] |
| DB-39 | `order_financial_lines` | `source_order_item_id`, `total_amount` | RW | `store_id` directo | ninguna | `split-allocation.util.ts:309-318` | ninguna línea del split apunta a un `order_items` con `cancelled_at` | `SELECT l.id FROM order_financial_lines l JOIN order_items i ON i.id=l.source_order_item_id WHERE i.cancelled_at IS NOT NULL` = 0 | [ ] |
| DB-40 | `fiscal_operation_events` | `event_type`, `resource_id` | W | fiscal-entity scoped; `accounting_entity_id` NOT NULL | ninguna | `pos-fiscal-emission.service.ts:632` | toda venta sin documento deja exactamente una fila del evento | `SELECT resource_id FROM fiscal_operation_events WHERE event_type='pos_sale_without_fiscal_document' GROUP BY 1 HAVING count(*)<>1` = 0 | [ ] |
| DB-41 | `invoice_retry_queue` | `invoice_id` (NOT NULL + FK) | W | sin getter en `StorePrismaService` | **REQUIERE DDL** — encolar venta sin factura exige `invoice_id` nullable + `order_id` (F-018a); fase I lo **acepta** | `pos-fiscal-emission.service.ts:578-590` | sin DDL este carril no inserta ninguna fila aquí | `SELECT count(*) FROM invoice_retry_queue` no crece tras esa venta | [ ] |
| DB-42 | `store_settings` | `settings.pos.tax_line_gate` legacy | R/W | `store_id` directo; `findFirst` | ninguna | `settings-schemas.dto.ts`, `payments.service.ts` | clave histórica aceptada pero inerte; default nuevo ausente | PATCH/GET legacy y cobro taxless 2xx con `block` | [ ] |
| DB-43 | `product_tax_assignments` | `tax_category_id`; sin historial | R | `products.store_id` | ninguna | `TaxesService.calculateProductTaxes` | sin fila actual = línea nueva taxless válida; no demuestra impuesto perdido | curl POS mesa nueva/existente sin asignación → 2xx | [ ] |
| DB-44 | `audit_logs` | `action`, `resource_id`, `metadata` | W | **getter devuelve `baseClient`** → sin scope; filtrar a mano | ninguna | `AuditService` desde `cancelDeliveredOrderItem` | toda cancelación de línea entregada deja fila con usuario, motivo y destino | `SELECT count(*) FROM audit_logs WHERE action='order_item.cancel_delivered' AND resource_id=:itemId` ≥ 1 | [ ] |
| DB-45 | `table_waiters` | pivote `table_id`↔`user_id` | R | rel. `table.store_id` | ninguna | `table-sessions.service.ts:1926-1999` proyecta hoy `waiter` desde este pivote; ADR-04 cambia la fuente a `opened_by` | el mesero mostrado sale de `opened_by`, no del pivote | `SELECT s.id FROM table_sessions s WHERE s.closed_at IS NULL AND s.opened_by IS NULL` = 0 | [ ] |
