# R.1 — F-003 Default de comentario PQR pasó a público fail-open → CONFIRMADO (major)

## Comandos

```bash
git diff origin/main..origin/develop -- apps/frontend/src/app/private/modules/store/pqr/pages/pqr-detail-page/pqr-detail-page.component.ts | grep -E '^[-+].*signal\('
sed -n '70,80p' apps/frontend/src/app/private/modules/store/pqr/pages/pqr-detail-page/pqr-detail-page.component.ts
grep -n "notify_requester" apps/frontend/src/app/private/modules/store/pqr/pages/pqr-detail-page/pqr-detail-page.component.ts
```

## Salida

```diff
-  readonly isInternal = signal(true);
-  readonly notifyRequester = signal(false);
+  readonly isInternal = signal(false);
+  readonly notifyRequester = signal(true);
```

```text
// Comment composer (working tree, fail-open)
readonly isInternal = signal(false);
readonly notifyRequester = signal(true);
197:      notify_requester: this.isInternal() ? this.notifyRequester() : true,
```

## Veredicto

**CONFIRMADO.** El diff flipa ambos defaults (`isInternal true→false`, `notifyRequester false→true`) y el working tree los conserva. La línea 197 fuerza `notify_requester: true` en todo comentario público: un operador que redacte una nota interna y olvide el toggle la publica **y** la envía por correo al solicitante.
