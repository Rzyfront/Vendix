# R.2 — F-012: Footer se guarda dos veces con doble toast

- **Verdict:** CONFIRMADO (minor, doble PATCH + doble toast en camino feliz)
- **Fecha:** 2026-09-11 · Rama: `develop` (solo lectura; sin cambios de código)

## Comando

```bash
sed -n '1555,1620p' apps/frontend/src/app/private/modules/store/ecommerce/ecommerce.component.ts
sed -n '1770,1815p' apps/frontend/src/app/private/modules/store/ecommerce/ecommerce.component.ts
```

## Salida

1. `onSubmit` (L1565): `const footerSaved = await this.saveFooterOnly();` y, si
   el form principal es válido, sigue al guardado general cuyo payload incluye
   `footer: this.footerSettings()` (L1605) vía `updateSettings(settings)`.
2. `saveFooterOnly` (L1790-1810): PATCH independiente
   `updateSettings({ footer })` + `toastService.success('Footer guardado')`;
   el guardado general emite después `'Configuración actualizada
   exitosamente'`. En el camino feliz (footer presente + form válido) hay dos
   PATCH con el footer y dos toasts de éxito por cada save.
3. La rama inválida sí está cuidada (early-return con un solo toast), así que
   el defecto es solo del camino feliz.

## Veredicto

CONFIRMADO. Fix según directiva: si `saveFooterOnly()` devolvió true, omitir
`footer` del payload general (o suprimir uno de los toasts). Comentar en
QUI-792.
