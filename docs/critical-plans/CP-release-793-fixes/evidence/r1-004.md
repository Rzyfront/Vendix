# R.1 — F-004 Toggle OFF habilitado pinta rojo en toda la app → CONFIRMADO (major, con directiva)

## Comandos

```bash
sed -n '40p' apps/frontend/src/app/shared/components/toggle/toggle.component.ts
sed -n '139,144p' apps/frontend/src/app/shared/components/toggle/toggle.component.ts
grep -rn "app-toggle" apps/frontend/src --include='*.html' | wc -l; grep -rln "app-toggle" apps/frontend/src --include='*.html' | wc -l
```

## Salida

```text
[style.background-color]="isDisabled() ? 'var(--color-muted)' : isOn() ? 'var(--color-primary)' : 'var(--color-danger)'"
// FIX QUI-801: antes se aplicaba `opacity-50` encima del gris muted, lo
// que daba un color apagado difícil de distinguir del estado "apagado
// pero habilitado". Ahora el template aplica `bg-[var(--color-danger)]`
// (rojo opaco) directamente al botón deshabilitado, así que acá solo
38 (usos en 19 templates)
```

## Veredicto

**CONFIRMADO.** La línea 40 pinta `danger` todo toggle OFF habilitado (settings, checkout, cualquier módulo): impacto app-wide (38 usos en 19 templates). El comentario QUI-801 describe además lo contrario de lo implementado (dice que el deshabilitado pinta rojo; el código pinta el deshabilitado `muted` y el OFF habilitado rojo). Directiva vigente: mantener el código y corregir el comentario (ADR-02, QUI-801).
