# R.2 — F-011: normalizeRatePercent cita espejo inexistente

- **Verdict:** CONFIRMADO (minor, solo-doc; lógica intacta)
- **Fecha:** 2026-09-11 · Rama: `develop` (solo lectura; sin cambios de código)

## Comando

```bash
rg -c "invoice-tax-catalog.service.ts" apps/backend/src
rg -n "toPercent" apps/frontend/src --max-count=40
sed -n '195,215p' apps/frontend/src/app/private/modules/store/invoicing/components/invoice-create/invoice-tax-catalog.service.ts
```

## Salida

1. `rg -c "invoice-tax-catalog.service.ts" apps/backend/src` → sin matches
   (vacío): el archivo espejo NO existe en backend. La cita del docblock
   (`invoice-line-math.ts:143`) apunta a la nada.
2. El `toPercent` real vive en frontend, dos sedes:
   - `invoice-create/invoice-tax-catalog.service.ts:203`
   - `invoice-create/invoice-withholding-catalog.service.ts:121`
   con la MISMA regla que el espejo: `value > 1 ? value : value * 100`
   (redondeo a 2 decimales). El guard es coherente con el contrato real del
   tax-selector y `resolveRateBasis`; solo la referencia cruzada miente
   (además cita `(F-013)`, tag de otro plan).

## Veredicto

CONFIRMADO como defecto exclusivamente documental. Fix: re-apuntar el docblock
al contrato real (`tax-selector` + `resolveRateBasis`, sede frontend), sin
tocar la lógica.
