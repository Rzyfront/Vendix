# R.1 — F-005 Checkout preselecciona primera tarifa contra comentario → CONFIRMADO (minor, con directiva)

## Comandos

```bash
git diff origin/main..origin/develop -- apps/frontend/src/app/private/modules/ecommerce/pages/checkout/checkout.component.ts | sed -n '/1727/,/preferred/p'
sed -n '1726,1733p;1763,1769p' apps/frontend/src/app/private/modules/ecommerce/pages/checkout/checkout.component.ts
sed -n '291,299p' apps/frontend/src/app/private/modules/ecommerce/pages/checkout/checkout.component.ts
```

## Salida

```diff
-            } else if (!stillValid) {
-              if (shippable.length === 1) {
-                this.selectShippingMethod(shippable[0], shippable[0].cost);
-              } else {
-                this.selected_shipping_option_id = null;   // limpiar = exigir elección
-                ...
+            } else if (!stillValid) {
+              // Preselección de tarifa en checkout: ... se preselecciona la primera (shippable[0]).
+              const preferred =
+                shippable.find((o: any) => o.postal_code_match) ?? shippable[0];
+              this.selectShippingMethod(preferred, preferred.cost);
```

Comentario superviviente (líneas 1726-1733) que exige lo contrario:

```text
// con 2+ (p. ej. Riohacha céntrica vs. alejada) se limpia la
// selección y el comprador elige explícitamente — preseleccionar
// la barata subcobra el envío remoto. `nextStep` bloquea avanzar
// sin selección, así que limpiar equivale a exigir la elección.
```

Gate (líneas 291-299): `canConfirmOrder` retorna `false` si `selected_shipping_method_id == null` — sigue existiendo, pero la preselección lo satisface siempre.

## Veredicto

**CONFIRMADO.** El PR cambió "con 2+ se limpia y el comprador elige" por `find(postal_code_match) ?? shippable[0]` + autoselección, dejando el comentario viejo que documenta la conducta anterior. No hay bypass del gate (sigue bloqueando sin método), pero la conducta documentada ya no existe. Directiva vigente: no tocar la lógica, solo reescribir el comentario (ADR-03).
