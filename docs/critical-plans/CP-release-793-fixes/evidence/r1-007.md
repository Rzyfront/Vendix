# R.1 — F-007 Tirilla POS sin 2 decimales pineados → CONFIRMADO (minor)

## Comandos

```bash
rg -n "toLocaleString" apps/backend/src/domains/store/print-formats/providers/pos-sale-ticket.provider.ts
sed -n '207,213p' apps/backend/src/domains/store/print-formats/providers/fiscal-document-print.mapper.ts
grep -n "5.000,00" apps/backend/src/domains/store/print-formats/providers/real-print-path.spec.ts
git diff origin/main..origin/develop -- apps/backend/src/domains/store/print-formats/providers/pos-sale-ticket.provider.ts | grep -E '^[-+].*toLocaleString'
node -e "console.log('bare :',(5000).toLocaleString('es-CO'));console.log('pinned:',(5000).toLocaleString('es-CO',{minimumFractionDigits:2,maximumFractionDigits:2}))"
```

## Salida

```text
# Override fiscal del provider (líneas 156,160,164,168 + 224-225,402-475): todo bare
model.totals.subtotal_formatted = `$${subtotal.toLocaleString('es-CO')}`;
# money() del mapper del mismo PR (fiscal-document-print.mapper.ts:210-213):
const money = (n: number) =>
  `$${Number(n || 0).toLocaleString('es-CO', {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  })}`;
# Spec (real-print-path.spec.ts:788):
expect(d.totals?.withholding_total_formatted).toBe('$5.000,00');
# Diff: las 4 líneas del override fiscal entran como `+` en este PR
bare : 5.000
pinned: 5.000,00
```

## Veredicto

**CONFIRMADO.** El override fiscal nuevo del provider formatea sin opciones (`$5.000`) mientras `money()` del mapper del mismo PR pinea 2 decimales (`$5.000,00`, exigido por el spec): mismo modelo, dos precisiones. Estilo preexistente del archivo, no regresión — por eso minor.
