# R.1 — F-001 Backticks sin escapar rompen ng build → CONFIRMADO (blocker)

## Comando

```bash
node -e "
const ts = require('./node_modules/typescript');
const fs = require('fs');
for (const f of ['apps/frontend/src/app/private/modules/store/invoicing/components/invoice-detail/invoice-detail.component.ts','apps/frontend/src/app/private/modules/store/invoicing/pages/invoice-create-page/invoice-create-page.component.ts']) {
  const src = fs.readFileSync(f,'utf8');
  const r = ts.transpileModule(src,{compilerOptions:{module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022},reportDiagnostics:true,fileName:f});
  const errs = (r.diagnostics||[]).filter(d=>d.category===1);
  console.log('FILE:',f,'errors:',errs.length);
  for (const d of errs) {
    const pos = d.file ? d.file.getLineAndCharacterOfPosition(d.start) : {line:'?',character:'?'};
    console.log(' TS'+d.code,'L'+(pos.line+1)+':'+(pos.character+1), ts.flattenDiagnosticMessageText(d.messageText,' '));
  }
}"
node -e "console.log('repo TS:',require('./node_modules/typescript').version)"
sed -n '726,728p' apps/frontend/src/app/private/modules/store/invoicing/components/invoice-detail/invoice-detail.component.ts
```

## Salida

```text
FILE: .../invoice-detail/invoice-detail.component.ts errors: 4
 TS1005 L726:43 ',' expected.
 TS1005 L726:48 ',' expected.
 TS1005 L727:57 ',' expected.
 TS1005 L1198:1 ',' expected.
FILE: .../invoice-create-page/invoice-create-page.component.ts errors: 7
 TS1005 L1550:35 ',' expected.
 TS1005 L1550:57 ',' expected.
 TS1005 L1551:52 ',' expected.
 TS1005 L3016:55 ',' expected.
 TS1005 L3016:64 ',' expected.
 TS1005 L3017:17 ',' expected.
 TS1135 L3515:1 Argument expression expected.
repo TS: 5.9.3
<!-- F-054: el `title` solo no lo lee el teclado ni la
     mayoría de lectores; el `aria-label` espeja el
     texto para quien no pasa el cursor. -->
```

## Veredicto

**CONFIRMADO.** 11 × TS1005 en las líneas exactas de r0 (invoice-detail:726,727,1198; invoice-create-page:1550,1551,3016,3017) con el TS del repo (5.9.3, vía independiente del `tsc` del revisor). Los backticks crudos de los comentarios HTML dentro de `template: \`...\`` cierran el literal. El TS1135 en L3515 (fin de archivo normal: `},`/`})`/`export class`) es cascada del mismo desbalance, no un bug independiente.
