---
id: F-034
round: 1
perspective: 5
severity: major
step: B.2
status: open
accepted_by: none
location: "apps/backend/src/domains/store/products/products.service.ts:1679"
updated: 2026-09-17
---
# F-034 — Fail-open no cubre throws de light/hydrate

- **Scenario:** ADR-03 solo degrada si `candidates.length > SCAN_CAP`; si la light o el hydrate lanzan (timeout bajo carga de keystrokes), la excepción llega al catch F-003 → 200+success:false sin `meta` → grid vacía silenciosa: exactamente lo que el paso jura prevenir.
- **Proposed fix:** Envolver todo el path rank (light→rank→slice→hydrate→re-sort): cualquier throw degrada al `orderBy` legacy + warn log (query-hash+store+latencia) + señal `meta.search` (F-012) + fila ERR + verificación matando la light.
- **Resolution:** —
