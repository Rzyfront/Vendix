---
id: F-043
round: 1
perspective: 7
severity: major
step: B.1
status: fixed
accepted_by: none
location: "apps/backend/src/domains/store/products/dto/index.ts:1227"
updated: 2026-09-17
---
# F-043 — Numéricos vacíos coercionan a 0

- **Scenario:** `?category_id=` → `Number('')=0` → pasa `@IsInt` → filtra por categoría inexistente 0 (listado vacío en vez de sin filtro); igual brand/store/page(→5xx)/limit(→take:0). Ningún caller garantiza omitir numéricos vacíos.
- **Proposed fix:** Idioma empty→undefined del mismo archivo en todos los numéricos query + `@Min(1)` paridad catálogo + specs `?category_id=`/`?page=`/`?limit=` pineados.
- **Resolution:** Fixed in B.1: empty->undefined numerics + @Min parity; category_id= unfiltered live.
