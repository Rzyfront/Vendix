---
id: F-089
round: 1
perspective: 8
severity: minor
step: B.2
status: fixed
accepted_by: none
location: "apps/backend/src/domains/store/products/products.service.ts:1893"
updated: 2026-09-17
---
# F-089 — Extras fijos por keystroke comen p95

- **Scenario:** Cada search paga además `resolvePosScope→loadMergedSettings` (:1737,4999) + 2º `loadMergedSettings` en `Promise.all` (:1893, posible duplicado) + `resolveActivePromotionsForListing` + `signUrl` por fila (:1918-1921): 20 sign + 2-3 queries auxiliares antes de contar search, todo contra los 250ms.
- **Proposed fix:** B.2 verifica memoización per-request de settings (dedupe si no), confirma `signUrl` HMAC local sin I/O (o lo presupuesta) e itemiza costos fijos en la evidencia p95 (search vs baseline findAll); nada visible se mueve.
- **Resolution:** Fixed in B.2: settings single-flight per request + signUrl audited (local SigV4, no I/O).
