---
id: F-040
round: 1
perspective: 7
severity: major
step: B.1
status: fixed
accepted_by: none
location: "apps/backend/src/domains/store/products/dto/index.ts:1242"
updated: 2026-09-17
---
# F-040 — pos_optimized=false coerciona a true

- **Scenario:** `@Type(() => Boolean)` + conversión implícita: `Boolean('false')===true` (trampa documentada en el propio DTO :1304-1313) → rama POS activa y `state` forzado a `active`: lo opuesto a lo pedido. El curl B.1 solo envía `true`, la rama false jamás se ejercita.
- **Proposed fix:** Migrar `pos_optimized` (mínimo) al patrón `@Transform` raw obj[key] del mismo archivo (:1314-1326) + spec `?pos_optimized=false` intacto; calendarizar 7 booleanos hermanos.
- **Resolution:** Fixed in B.1: pos_optimized migrated to raw @Transform pattern; false==absent live.
