---
id: F-030
round: 1
perspective: 4
severity: major
step: C.3
status: open
accepted_by: none
location: "apps/backend/src/prisma/base/base-prisma.service.ts:55"
updated: 2026-09-17
---
# F-030 — $queryRaw nombrado pero solo existe $queryRawUnsafe

- **Scenario:** ADR-04/C.3 prometen "parametrizado $queryRaw" pero el servicio solo expone `$queryRawUnsafe(query, ...values)`: el implementador o no compila o interpola tokens al string → inyección SQL en path de keystroke. Consolida P6-F3.
- **Proposed fix:** Contrato a placeholders (`$1..$n`, cero `${}` de input), gate grep que rechaza interpolación en `searchIdsRanked`, spec con tokens `' OR '1'='1`, `\`, `%` literales.
- **Resolution:** —
