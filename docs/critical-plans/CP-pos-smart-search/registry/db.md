# Database Contract Registry

| Id | Model / table | Columns | R/W | Tenant scoping | Migration | Consumers | Invariant | Verification | Status |
|----|---------------|---------|-----|----------------|-----------|-----------|-----------|--------------|--------|
| DB-01 | products (where) | name,desc,sku contains-insens; state,brand,flags,id | R | dir: store_id inyectado StorePrismaService | — | findAll,findIds,resolveBestSellingPageIds | search⊗barcode excluyentes; pos_optimized→state=active | EXPLAIN SELECT * FROM products WHERE store_id=1 AND name ILIKE '%cafe%'; | [ ] |
| DB-02 | products (barcode OR) | barcode exacto: products, variants, tier-assignments | R | dir store_id products; relacional en variantes/tiers | — | findAll,findIds vía buildProductWhere | barcode es equals exacto; scan resuelve 3 espacios | SELECT id FROM products WHERE store_id=1 AND barcode='X'; | [ ] |
| DB-03 | product_variants (include) | id,sku,barcode,name,precios,márgenes,flags,attrs + stock + images | R | rel: variants→products.store_id (scoped_client) | — | pos_optimized forzado + include_variants/barcode | disponibilidad=SUM(stock.quantity_available), NUNCA variant.stock_quantity | SELECT * FROM product_variants pv JOIN products p ON p.id=pv.product_id WHERE p.store_id=1; | [ ] |
| DB-04 | stock_levels (include) | variant_id,qty_available,qty_reserved + locations(id,name,type) | R | rel vía locations.store_id + displayable∩sellable | — | findAll (forzado si pos_optimized) | display⊆committable QUI-559; vender usa available, no on_hand | SELECT * FROM stock_levels sl JOIN inventory_locations l ON l.id=sl.location_id WHERE l.store_id=1 AND l.is_active; | [ ] |
| DB-05 | inventory_locations (filtro) | store_id,is_active,is_central_warehouse,type∉{quarantine,damaged} | R | dir store_id; central store_id NULL + CHECK | — | sellable/displayableWhere, resolvePosStockScope | central nunca vende POS; main sin default degrada a all_locations | SELECT id FROM inventory_locations WHERE store_id=1 AND is_active AND NOT is_central_warehouse; | [ ] |
| DB-06 | price_tier_assignments | price_tier_id,barcode; unique/tienda índice parcial | R | rel product.store_id (scoped_client) | parcial UNIQUE(store_id,barcode) WHERE NN (fuera schema) | buildProductWhere, findAll, scanned_tier | barcode presentación único/tienda; is_default único/producto | SELECT indexname FROM pg_indexes WHERE tablename='product_price_tier_assignments'; | [ ] |
| DB-07 | order_items (ranking) | groupBy product_id, SUM(qty), 30d, COMPLETED_SALE_STATES, take 200, Redis 24h | R | rel orders.store_id; fail-open→mapa vacío | — | resolveBestSellingRank si best_selling_first | nunca vacía grilla; cat>5000→null+orden barato | SELECT product_id,SUM(quantity) FROM order_items oi JOIN orders o ON o.id=oi.order_id WHERE o.store_id=1 GROUP BY 1; | [ ] |
| DB-08 | stores (scope-ref) | default_location_id vía findUnique + storeId RequestContext | R | SIN scope: getter=baseClient; filtro manual obligatorio | — | loadStoreScopeRef (solo pos_optimized) | lecturas stores filtran store_id a mano; sin contexto→{default_location_id:null} | código: store-prisma.service.ts:1051 getter sin scope | [ ] |
| DB-09 | settings + joins include | store_settings scoped; brands,categories,stores,images(is_main×1),tax→rates | R | dir store_id settings/brands/cats; rel images/tax vía products | — | findAll siempre; pos_optimized+calculateFinalPrice | _count(variants,images,reviews) incluido; images take 1 is_main | SELECT * FROM store_settings WHERE store_id=1; | [ ] |
| DB-10 | ai_embeddings (RAG) | store_id,org,etype,eid,content,embedding(1536),meta; HNSW | R-pot | dir store_id; vector ops solo raw SQL | 20260326030000_add_ai_embeddings (vector+HNSW) | ninguno en POS hoy; alternativa semántica | vectorial exige filtro manual store_id+entity_type='product' | SELECT entity_id FROM ai_embeddings WHERE store_id=1 AND entity_type='product' LIMIT 1; | [ ] |
| DB-11 | scope / $queryRaw | StorePrismaService extiende Base; scope mezcla store_id/rel en where+create | R/W | Global=sin scope; $queryRaw(Unsafe)+withoutScope BYPASSAN scope | — | todo backend | raw SQL=tenant manual obligatorio; findUnique scopeado puede romper→findFirst | código: base-prisma.service.ts:55-68 | [ ] |
| DB-12 | índices products | uniques(store_id,slug/sku/barcode); idx(store_id,sku/barcode/type/ing/state/feat/cons),(state,id) | — | — | schema L2022-2042 | optimizador filtros exactos/tienda | SIN índice name/description → contains=seq scan; justifica trigram/GIN | SELECT indexname FROM pg_indexes WHERE tablename='products'; | [ ] |
| DB-13 | extensiones ON | pgcrypto (20260428004051), vector (20260326030000) | — | N/A globales | patrón repo: CREATE EXTENSION IF NOT EXISTS | — | nuevas extensiones siguen IF NOT EXISTS | SELECT extname FROM pg_extension; | [ ] |
| DB-14 | PROP pg_trgm+unaccent | similitud trigram + sin tildes (cafe=café) | — | N/A globales | PROP aditiva: 0 filas, sin FK, IF NOT EXISTS; aprobación pdte | motor smart-search | reglas ñ custom (C.1); trigram sin GIN no acelera (→DB-16) | SELECT * FROM pg_available_extensions WHERE name IN ('pg_trgm','unaccent'); | [ ] |
| DB-16 | PROP GIN trigram | GIN por expresión: unaccent(lower(name)), igual sku | — | BitmapAnd con btree store_id (tenant-pruning) | PROP aditiva; INVALID→DROP-or-REINDEX | motor smart-search | expr canónica ≡ C.3; gates: build≤30min, GIN≤3x tabla | SELECT pg_relation_size('products'); | [ ] |
| DB-17 | where único | buildProductWhere(:1360 privada): llamadores findAll:1732, findIds:1464, resolveBestSellingPageIds | R | hereda scope llamador (StorePrismaService) | — | GET /store/products, /ids, selección masiva UI | "select-all" = conjunto visible exacto; ids se combina con state | grep -rn "buildProductWhere\|findIds" apps/backend/src/domains/store/products/ | [ ] |
