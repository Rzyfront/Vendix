# Frontend↔Backend Contract Registry

| Id | Method + route | Request DTO | Response shape | Frontend consumer | Change | Risk | Verification | Status |
|----|----------------|-------------|----------------|-------------------|--------|------|--------------|--------|
| FB-01 | GET /store/products | ProductQueryDto: search,barcode,pos_opt,state,filtros,pag | {success,data[],meta}; row=prod+variants+stock+img | pos-product.service ← selection.component | tokenizado+unaccent+ranking+meta.search | M: cambia orden | curl "$API/store/products?search=cafe%20chocolate&pos_optimized=true" -H"Authorization: Bearer $T"\|jq '.data\|length' | [ ] |
| FB-02 | GET /store/products?barcode= | barcode: equals exacto producto/variante/tier; anula search | envelope FB-01, 1 fila + scanned_price_tier_id | pos getProductByBarcode → firstTransformedProduct | none (regression check only) | B: no tocar rama barcode | curl "$API/store/products?barcode=7701234" -H"Authorization: Bearer $T"\|jq .data[0].name | [ ] |
| FB-03 | GET /store/products?sku= | sku NO en DTO → forbidNonWhitelisted 400 | n/a (siempre null hoy) | pos getProductBySku (catch→null), sin callers | none; HALLAZGO: muerto en práctica | B: documentar, no arreglar | curl -o/dev/null -w%{http_code} "$API/store/products?sku=X" -H"Authorization: Bearer $T" =400 | [ ] |
| FB-04 | GET /store/products | DTO admin: state,is_ingredient,cat/brand_id,search,ids | envelope; fila admin (costos,tax_map,stock) | products.service getProducts + invoice-product-lookup | hereda ranking FB-01 | M: con search ahora rankeado; sin search intacto | curl "$API/store/products?search=tornillo&state=active" -H"Authorization: Bearer $T"\|jq .meta.total | [ ] |
| FB-05 | GET /store/products + /ids | DTO bulk: search?,state,ids CSV,page,limit=total | envelope + /ids:{ids[],total,capped} | products-bulk-edit.service getProductsPage/getIds | hereda ranking FB-01 | B: /ids debe usar MISMO where | curl "$API/store/products/ids?search=tornillo" -H"Authorization: Bearer $T"\|jq .data.total = FB-04 | [ ] |
| FB-06 | GET /store/products | DTO: search,is_sellable,include_variants,featured_first,pag | envelope; consume data+pagination | add-items-modal restaurant (scroll infinito) | hereda ranking FB-01 | B | UI: modal en mesa, buscar "pollo", 1er resultado es el plato | [ ] |
| FB-07 | GET /store/products | DTO: search?,limit=200 | envelope; filtra cliente is_ingredient\|\|is_sellable | recipe-ingredients.service listIngredients | hereda ranking FB-01 | B | curl "$API/store/products?search=harina&limit=200" -H"Authorization: Bearer $T"\|jq '.data[0].name' | [ ] |
| FB-08 | GET /store/products | DTO SIN search: product_type=service,requires_booking | envelope; lee res.data directo | reservations.service + 2 modales + pos-quick-book | none (regression check only) | B: sin search no cambia | UI: picker servicios reserva, lista no vacía | [ ] |
| FB-09 | GET /store/products?limit=200 | sin search | envelope | template-editor + templates.component | none (regression check only) | Nula | UI: editor plantillas, selector productos carga | [ ] |
| FB-10 | GET .../adjustments/search-products | raw: search,location_id,limit=10 (DTO huérfano) | success([id,name,sku,stock@loc]) | inventory.service ← scanner-modal | hermano mismo defecto (contains sin tokenizar) | M si se incluye; scope decide | curl "$API/store/inventory/adjustments/search-products?search=cafe&location_id=1" -H"Authorization: Bearer $T"\|jq '.data\|length' | [ ] |
| FB-11 | GET .../stock-transfers/search-products | raw: search,from/to_loc,limit=10 (DTO muerto) | {success,data:[...]} sin ResponseService | transfers.service searchTransferable | mismo defecto (contains + stock@origen) | M si se incluye | curl "$API/store/stock-transfers/search-products?search=cafe&from_location_id=1&to_location_id=2" -H"Authorization: Bearer $T"\|jq '.data\|length' | [ ] |
| FB-12 | GET /ecommerce/catalog?search= | CatalogQuery: search,ids,cat/brand,min/max_price,sort_by,pag + x-store-id | {success,data:EcommerceProduct[],meta} | catalog.service ← catalog page + search-autocomplete | hermano público defecto análogo | A si se toca: pública sin auth; FUERA scope | curl "$API/ecommerce/catalog?search=cafe&limit=5" -H"x-store-id: 1"\|jq '.data\|length' | [ ] |
| FB-13 | Rutas INEXISTENTES backend | — | 404 Nest (o :id→400 ParseInt) | pos-api.service by-barcode/by-sku/:id/stock + admin /search,/category — MUERTOS | none (NO revivir aquí) | Nula: confirmar CP no los toca | curl -o/dev/null -w%{http_code} "$API/store/products/by-barcode/X" -H"Authorization: Bearer $T" =404/400 | [ ] |
| FB-14 | GET /store/products (mobile) | DTO: search,state=active,include_variants,pos_optimized | envelope FB-01 | mobile ProductService.search/list ← pos/products/purchase/adjust/transfers | hereda ranking FB-01 | M: POS móvil 2do consumidor crítico | App móvil: buscar cafe + filtro precio → 200 rankeado igual web | [ ] |
| FB-15 | findAll vía Vexi search_products | {search} interno (products.tools.ts:766) | envelope FB-01 (server-side) | ai-engine products.tools.ts:758-776 | hereda recall+ranking nuevos | B: Vexi gana recall sin cambiar código | Vexi: "agrega café" resuelve producto correcto en POS | [ ] |
