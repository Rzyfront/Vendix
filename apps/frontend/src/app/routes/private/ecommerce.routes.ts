import { Routes } from '@angular/router';

/**
 * E-commerce private routes for authenticated customers.
 * These routes are loaded when environment is STORE_ECOMMERCE.
 *
 * NOTE: Account, checkout, and orders routes have been moved to
 * store_ecommerce.public.routes.ts to ensure they render within
 * the store-ecommerce layout wrapper.
 */
export const ecommerceRoutes: Routes = [];

