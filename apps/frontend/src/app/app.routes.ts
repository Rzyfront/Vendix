import { Routes } from '@angular/router';

/**
 * Rutas iniciales de la aplicación.
 * Este array estará vacío, ya que el RouteManagerService se encargará
 * de añadir dinámicamente las rutas correctas durante la inicialización.
 */
export const routes: Routes = [];